# Nacos配置
nacos:
  default: # Nacos ID
    url: http://localhost:8848
    username: nacos
    password: nacos
    blacklist: # 黑名单，不允许修改的配置
      - namespace: public
        group: DEFAULT_GROUP
        dataId: sss:ssss

# 数据库配置
database:
  db1: # Database ID
    url: localhost
    port: 3306
    database: test
    username: root
    password: "1234"

# 日志配置
logging:
  level: INFO
  format: "[%(asctime)s] %(levelname)s %(name)s %(message)s"
