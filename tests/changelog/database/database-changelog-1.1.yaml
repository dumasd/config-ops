databaseChangeLog:
- changeSet:
    author: jack.hua
    changes:
    - sql: {sql: DELETE FROM company WHERE id > 100; DELETE FROM company WHERE id
          < 12;}
    comment: example-comment
    context: gradle-example
    id: database-changelog-1.1-1111
    labels: before
- changeSet:
    author: jack.hua
    changes:
    - sql: {sql: DELETE FROM company WHERE id > 100;}
    comment: example-comment
    context: gradle-example
    id: database-changelog-1.1-1112
    labels: before
