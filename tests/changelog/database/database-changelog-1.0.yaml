databaseChangeLog:
- changeSet:
    author: bruce.wu
    changes:
    - sql: {endDelimiter: ;, sql: "CREATE TABLE IF NOT EXISTS `company` (\n  `id`
          int NOT NULL AUTO_INCREMENT,\n  `name` varchar(50) COLLATE utf8mb4_bin NOT
          NULL,\n  `address1` varchar(50) COLLATE utf8mb4_bin DEFAULT NULL,\n  `address2`
          varchar(50) COLLATE utf8mb4_bin DEFAULT NULL,\n  `city` varchar(30) COLLATE
          utf8mb4_bin DEFAULT NULL,\n  PRIMARY KEY (`id`)\n) ENGINE=InnoDB;"}
    comment: example-comment
    context: gradle-example
    id: database-changelog-1.0
    labels: gradle-example
    runInTransaction: true
